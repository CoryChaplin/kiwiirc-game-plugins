{
  "_comment": "Games included in the bundle. false = excluded from the build. CLI override: yarn build --env exclude=chess,pictionary  |  yarn build --env config=./build.custom.json",
  "games": {
    "tictactoe": true,
    "connectfour": true,
    "pictionary": true,
    "battleship": true,
    "chess": true
  }
}
